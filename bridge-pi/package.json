{
  "name": "cast-away-pi",
  "version": "2.0.1",
  "description": "Cast Away — Chromecast screensaver bridge for Raspberry Pi",
  "private": true,
  "scripts": {
    "start:engine": "cd engine && node index.js",
    "install:engine": "cd engine && npm install --production"
  }
}
